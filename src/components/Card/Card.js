import React from 'react';

const Card = ({card}) => {
  console.log(card)
  debugger
  // const createCards = (card) => {
  //   console.log(Object.keys(card.stats))
  //   // const tags = Object.keys(card.stats).map(stat => <p>{card[stat]}</p>)
  //   debugger
  // return (
  //   <article>
  //     <h1>{card.name}</h1>
      
  //     <button>Bookmark</button>
  //   </article>
  //   )
  // }

  return (
    <div className='Card'>
    </div>
  )

}

export default Card;